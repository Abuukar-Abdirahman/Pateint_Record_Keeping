@model TestingPatient.Models.MedicalRecord

@{
    ViewData["Title"] = "Details";
    Layout = "_AdminLayout";
}

<h1 class="h3 mb-4 text-gray-800">Medical Record Details</h1>

<div class="card shadow mb-4">
    <div class="card-header py-3 bg-gradient-primary text-white">
        <h6 class="m-0 font-weight-bold text-white">Medical Record Information</h6>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-2">Record Date</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.RecordDate)</dd>

            <dt class="col-sm-2">Description</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Description)</dd>

            <dt class="col-sm-2">Patient</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Patient.Name)</dd>
        </dl>
    </div>
</div>

<!-- Edit Button -->
<div class="d-flex justify-content-between mt-3">
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning btn-lg">
        <i class="fas fa-edit"></i> Edit Medical Record
    </a>
</div>
